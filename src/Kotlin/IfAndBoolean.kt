package Kotlin

fun main() {
    /**
     * When booleans are null, in trying to use them in an if statement, one needs to check if they are truly null
     *
     */
    val isAdult = true

    if (isAdult == true){
        println("this is a very true statement")
    }else {
        println("go fuck yourself")
    }
}
